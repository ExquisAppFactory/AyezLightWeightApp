server:
  port: 8301

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8302/eureka

spring:
  application:
    name: cloud-gateway
  cloud:
    gateway:
      routes:
        - id: userModule
          uri: lb://USER-SERVICE
          predicates:
            - Path=/user/**
